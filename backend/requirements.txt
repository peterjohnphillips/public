# Version floors rather than exact pins: this project targets Python 3.14,
# and the floors below are the first releases with cp314 wheels.
#
# Only used by scripts/build_content.py (the Markdown -> content.json parser)
# and its tests. The shipped app has no Python runtime dependency.
pydantic>=2.12
python-frontmatter>=1.1
PyYAML>=6.0.3

# dev / test
pytest>=8.4
