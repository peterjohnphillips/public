# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Node
node_modules/
dist/
.vite/
*.local

# App data (generated at runtime)
data/
*.db
*.db-journal

# Editors / OS
.claude/settings.local.json
*.tmp
.vscode/
.idea/
.DS_Store
Thumbs.db

# TS build cache
*.tsbuildinfo

# Static Pages build: the one artifact is /index.html, served from the branch
# root. It is committed, as is frontend/src/generated/content.json (derived
# from content/ by scripts/build_content.py, but checked in so `npm run build`
# works without the Python step).
